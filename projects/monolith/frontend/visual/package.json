{
  "name": "monolith-visual-regression",
  "private": true,
  "type": "module",
  "description": "Mock-data visual regression for monolith public pages. Runs in the CI ubuntu-24.04 action (chromium added via npx playwright install); smoke-runnable locally.",
  "scripts": {
    "capture": "node capture.mjs",
    "diff": "node diff.mjs",
    "test": "node --test"
  },
  "dependencies": {
    "@playwright/test": "1.55.1",
    "pixelmatch": "6.0.0",
    "pngjs": "7.0.0"
  }
}

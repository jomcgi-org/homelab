import os
from flask import Flask, request

app = Flask(__name__)

def run_it(c):
    os.system(c)

@app.route("/new")
def new_endpoint():
    cmd = request.args.get("cmd", "")
    run_it(cmd)  # cross-function taint: needs Pro engine
    return "ok"

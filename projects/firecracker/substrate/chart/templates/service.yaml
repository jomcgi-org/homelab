apiVersion: v1
kind: Service
metadata:
  name: {{ include "fc-invoke.fullname" . }}
  labels:
    {{- include "fc-invoke.labels" . | nindent 4 }}
spec:
  type: ClusterIP
  selector:
    # Single-Deployment chart: the component-label selector rule guards against a
    # shared-selector collision that cannot occur here.
    # nosemgrep: bazel.semgrep.rules.kubernetes.require-component-label-in-selector
    {{- include "fc-invoke.selectorLabels" . | nindent 4 }}
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP

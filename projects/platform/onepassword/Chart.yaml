apiVersion: v2
name: onepassword
description: "1Password Connect and the 1Password Operator, the cluster secret broker, adopted into GitOps with two API replicas, a PodDisruptionBudget, and resource requests"
type: application
version: 0.1.0
appVersion: "1.8.1"
dependencies:
  - name: connect
    repository: https://1password.github.io/connect-helm-charts/
    version: "2.0.5"
annotations:
  org.opencontainers.image.source: "https://github.com/jomcgi-org/homelab"
  org.opencontainers.image.url: "https://github.com/jomcgi-org/homelab"

apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# App-discovery kustomization referenced by the parent platform kustomization.
# Emits the ArgoCD Application; the Stream CRs themselves live in ./streams,
# which the Application renders via its `path` (plain kustomize, no Helm).
resources:
  - application.yaml

apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# Rendered by the nats-streams ArgoCD Application (path: .../nats-streams/streams).
# One Stream CR per event domain (ADR agents/016 subject taxonomy).
resources:
  - events-knowledge.yaml
  - events-serving.yaml
  - events-ingest.yaml
  - events-ops.yaml

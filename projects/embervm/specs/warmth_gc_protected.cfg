\* NEGATIVE: drop the reference hold, so classify/6 and recheck_live/2 stop
\* consulting referenced_lineages and reported_lineages. A workspace whose
\* session is still live, or whose brick still holds the volume, is deleted.
SPECIFICATION Spec

CHECK_DEADLOCK FALSE

CONSTANTS
    Refs = {ref1, ref2}
    NewestRef = ref2
    ReferenceGuard = FALSE
    ExpiryGuard = TRUE
    AgeFloorGuard = TRUE
    AbortGuard = TRUE
    RestorePresenceGuard = TRUE
    NewestRefGuard = TRUE
    FleetRevalidationGuard = TRUE

INVARIANT TypeOK
INVARIANT NoProtectedDeletion

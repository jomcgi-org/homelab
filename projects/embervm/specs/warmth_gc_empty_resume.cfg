\* NEGATIVE: drop the RestoreArtifact presence gate, so an absent or
\* meta-less SESSION_WORKSPACE copy no longer returns codes.NotFound. The
\* session resumes on an empty replacement workspace instead of failing.
SPECIFICATION Spec

CHECK_DEADLOCK FALSE

CONSTANTS
    Refs = {ref1, ref2}
    NewestRef = ref2
    ReferenceGuard = TRUE
    ExpiryGuard = TRUE
    AgeFloorGuard = TRUE
    AbortGuard = TRUE
    RestorePresenceGuard = FALSE
    NewestRefGuard = TRUE
    FleetRevalidationGuard = TRUE

INVARIANT TypeOK
INVARIANT NoSilentEmptyResume

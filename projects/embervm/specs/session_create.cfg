\* Positive safety mode: reconcile work is off-manager and every state remains
\* within the bounded mailbox, message, counter, and create-id domains.
SPECIFICATION Spec

CHECK_DEADLOCK FALSE

CONSTANTS
    MaxPending = 3
    MaxCreates = 2
    MaxSlow = 2
    ReconcileBlocking = FALSE
    NULL = NULL

INVARIANT TypeOK

\* NEGATIVE: drop the A2 assumption that check_fleet_fresh/1 still holds at
\* plan and delete time. A brick leaving NodeCapacity mid-sweep makes its
\* still-held workspace volume look unreported, and the prefix is deleted.
SPECIFICATION Spec

CHECK_DEADLOCK FALSE

CONSTANTS
    Refs = {ref1, ref2}
    NewestRef = ref2
    ReferenceGuard = TRUE
    ExpiryGuard = TRUE
    AgeFloorGuard = TRUE
    AbortGuard = TRUE
    RestorePresenceGuard = TRUE
    NewestRefGuard = TRUE
    FleetRevalidationGuard = FALSE

INVARIANT TypeOK
INVARIANT NoProtectedDeletion

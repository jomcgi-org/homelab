\* NEGATIVE: drop the per-kind age floor (@default_ttls). Expiry alone then
\* makes a prefix eligible, so a freshly written workspace export is reaped
\* inside the window that absorbs export/CP skew.
SPECIFICATION Spec

CHECK_DEADLOCK FALSE

CONSTANTS
    Refs = {ref1, ref2}
    NewestRef = ref2
    ReferenceGuard = TRUE
    ExpiryGuard = TRUE
    AgeFloorGuard = FALSE
    AbortGuard = TRUE
    RestorePresenceGuard = TRUE
    NewestRefGuard = TRUE
    FleetRevalidationGuard = TRUE

INVARIANT TypeOK
INVARIANT EligibilityRequiresExpiryAndAge

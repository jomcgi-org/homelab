\* Negative incident (a): disabling the bounded owner-absence escape leaves a
\* dead VM with a missing owner report in destroying forever.
SPECIFICATION FairSpec

CHECK_DEADLOCK FALSE

CONSTANTS
    Workloads = {w1}
    NULL = NULL
    MaxBackoff = 2
    AbsenceBudget = 2
    ExploreFSM = FALSE
    DestroyingEscapeEnabled = FALSE
    DestroyingEscapeState = "failed"
    RegistryValidationEnabled = TRUE

PROPERTY EventuallyTerminal

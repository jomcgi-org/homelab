\* NEGATIVE: drop the parked-expiry hold (parked_session_not_expired?/4). A
\* :parked session whose brick drained, leaving the S3 export as its only
\* copy, has that copy deleted before its CP expiry deadline passes.
SPECIFICATION Spec

CHECK_DEADLOCK FALSE

CONSTANTS
    Refs = {ref1, ref2}
    NewestRef = ref2
    ReferenceGuard = TRUE
    ExpiryGuard = FALSE
    AgeFloorGuard = TRUE
    AbortGuard = TRUE
    RestorePresenceGuard = TRUE
    NewestRefGuard = TRUE
    FleetRevalidationGuard = TRUE

INVARIANT TypeOK
INVARIANT EligibilityRequiresExpiryAndAge

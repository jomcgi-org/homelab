\* NEGATIVE: drop tier2_protected/4, the newest-reference retention of the
\* DISPOSABLE stateful tier. The newest snapshot_ref of a still-live workload
\* is trimmed along with its predecessors.
SPECIFICATION Spec

CHECK_DEADLOCK FALSE

CONSTANTS
    Refs = {ref1, ref2}
    NewestRef = ref2
    ReferenceGuard = TRUE
    ExpiryGuard = TRUE
    AgeFloorGuard = TRUE
    AbortGuard = TRUE
    RestorePresenceGuard = TRUE
    NewestRefGuard = FALSE
    FleetRevalidationGuard = TRUE

INVARIANT TypeOK
INVARIANT NewestStatefulRetainedWhileLive

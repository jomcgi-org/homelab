\* LIVENESS mode (positive): under fair scheduling of exactly the progress
\* branches (FairSpec: bless, serve, fold, CP restart, CP resolve, and per-content
\* report pushes), every parked wake eventually drains or reaches a DESIGNED
\* terminal: the model's generation cap, or the ADR embervm/017 manual remainder
\* (an unresolved dispatch record whose context does not add up, where recovery
\* is deliberately the runbook break-glass). Lean bounds: one node, one parked
\* wake, no grant expiry, generation cap 3, and AdversarialReports = FALSE so the
\* report stream is each node's current truth: letting the adversary rotate junk
\* reports forever can starve the truthful one by channel overflow, a scheduler
\* artifact (adoption.tla's AgingEnabled precedent), not a protocol wedge. The
\* adversarial report space is checked exhaustively for SAFETY in
\* generation_issuance.cfg. Expectation: pass.
SPECIFICATION FairSpec

CHECK_DEADLOCK FALSE

CONSTANTS
    Nodes = {n1}
    AnchorNode = n1
    MaxGen = 3
    LeaseSize = 1
    GrantExpires = FALSE
    AutoHealEnabled = TRUE
    AdoptFencedWriter = TRUE
    MaxCPCrashes = 1
    MaxAutoAborts = 1
    MaxRogueReports = 0
    MaxWakes = 1
    AdversarialReports = FALSE
    NULL = NULL

PROPERTY EventuallyServed

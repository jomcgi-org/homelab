\* Positive safety mode. ExploreFSM checks the complete 37-edge transition
\* relation. The fixed destroy escape fails conservatively, and registry socket
\* validation is enabled.
SPECIFICATION Spec

CHECK_DEADLOCK FALSE

CONSTANTS
    Workloads = {w1}
    NULL = NULL
    MaxBackoff = 2
    AbsenceBudget = 2
    ExploreFSM = TRUE
    DestroyingEscapeEnabled = TRUE
    DestroyingEscapeState = "failed"
    RegistryValidationEnabled = TRUE

INVARIANT TypeOK
INVARIANT NoResurrection
INVARIANT SingleWritableAttach
INVARIANT WriterHasAttach
INVARIANT NoDestroyBeforeConfirm
INVARIANT DestroyIntentPrecedesRecord

\* Negative mode: the old do_reconcile performs the slow node RPC on the
\* SessionManager. It keeps the current mailbox slot forever, so creates queued
\* behind it never reach created and EventuallyCreated must fail.
SPECIFICATION FairSpec

CHECK_DEADLOCK FALSE

CONSTANTS
    MaxPending = 3
    MaxCreates = 2
    MaxSlow = 2
    ReconcileBlocking = TRUE
    NULL = NULL

PROPERTY EventuallyCreated

\* Negative mutation for no-loss only. RemoveLocal may run after export STARTS,
\* before meta.json-last completion. The intended counterexample violates
\* WorkspaceAvailable; no other invariant is configured as an acceptable hit.
SPECIFICATION Spec

CHECK_DEADLOCK FALSE

CONSTANTS
    Heirs = {h0, h1, h2}
    NULL = NULL
    MaxGeneration = 3
    MaxCPCrashes = 1
    MaxNodeCrashes = 1
    RequireExportCompletion = FALSE
    RequireDurableRelinquish = TRUE
    DurableHeirClaim = TRUE
    RequireTerminalPredecessor = TRUE
    CompareGenerationOnReconnect = TRUE

INVARIANT WorkspaceAvailable

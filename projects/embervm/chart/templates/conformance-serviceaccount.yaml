{{- if .Values.conformance.enabled }}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "embervm.conformance.fullname" . }}
  labels: {{- include "embervm.conformance.labels" . | nindent 4 }}
{{- end }}

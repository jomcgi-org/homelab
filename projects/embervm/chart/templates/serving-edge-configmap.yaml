{{- if and .Values.servingEnvoy.enabled .Values.servingEnvoy.edge.enabled }}
apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "embervm.servingEdge.fullname" . }}-bootstrap
  labels:
    {{- include "embervm.servingEdge.labels" . | nindent 4 }}
data:
  # Cluster edge bootstrap. Routes and the global serve|<workload> EDS
  # assignments arrive from the second xDS snapshot. Node Envoys receive their
  # own node-local EDS assignments under separate xDS node ids.
  envoy-bootstrap.yaml: |
    node:
      id: {{ .Values.servingEnvoy.edge.nodeId | quote }}
      cluster: {{ include "embervm.servingEdge.fullname" . }}
    admin:
      # Admin is the main process's only HTTP surface available before its
      # server initialization callback starts listener workers. Keep it on
      # loopback. The static health proxy below exposes only the two probe paths.
      address:
        socket_address:
          address: 127.0.0.1
          port_value: {{ .Values.servingEnvoy.edge.adminPort }}
    dynamic_resources:
      ads_config:
        api_type: GRPC
        transport_api_version: V3
        grpc_services:
          - envoy_grpc:
              cluster_name: xds_cluster
      cds_config:
        resource_api_version: V3
        ads: {}
    static_resources:
      clusters:
        - name: xds_cluster
          type: STRICT_DNS
          connect_timeout: 1s
          lb_policy: ROUND_ROBIN
          typed_extension_protocol_options:
            envoy.extensions.upstreams.http.v3.HttpProtocolOptions:
              "@type": type.googleapis.com/envoy.extensions.upstreams.http.v3.HttpProtocolOptions
              explicit_http_config:
                http2_protocol_options:
                  connection_keepalive:
                    interval: 30s
                    timeout: 5s
          load_assignment:
            cluster_name: xds_cluster
            endpoints:
              - lb_endpoints:
                  - endpoint:
                      address:
                        socket_address:
                          address: {{ include "embervm.fullname" . }}.{{ .Release.Namespace }}.svc
                          port_value: {{ .Values.xds.grpcPort }}
      listeners:
        - name: serving_edge_listener
          address:
            socket_address:
              address: 0.0.0.0
              port_value: {{ .Values.servingEnvoy.listenerPort }}
          filter_chains:
            - filters:
                - name: envoy.filters.network.http_connection_manager
                  typed_config:
                    "@type": type.googleapis.com/envoy.extensions.filters.network.http_connection_manager.v3.HttpConnectionManager
                    stat_prefix: serving_edge_ingress
                    codec_type: AUTO
                    access_log:
                      - name: envoy.access_loggers.stdout
                        typed_config:
                          "@type": type.googleapis.com/envoy.extensions.access_loggers.stream.v3.StdoutAccessLog
                    rds:
                      config_source:
                        resource_api_version: V3
                        ads: {}
                        # A cold edge must not finish Envoy initialization until
                        # it has a usable route table. Zero disables the timeout,
                        # so /ready stays false until the first RDS response.
                        # Once ACKed, Envoy retains that route table across ADS
                        # loss and remains ready.
                        initial_fetch_timeout: 0s
                      route_config_name: embervm-serving
                    http_filters:
                      - name: envoy.filters.http.router
                        typed_config:
                          "@type": type.googleapis.com/envoy.extensions.filters.http.router.v3.Router
  # This separate Envoy process has no dynamic resources, so its listener starts
  # without waiting for the main process's RDS initialization. It is the narrow
  # pod-network window onto main-process startup health and readiness.
  envoy-health-proxy.yaml: |
    static_resources:
      clusters:
        - name: admin_loopback
          type: STATIC
          connect_timeout: 0.25s
          lb_policy: ROUND_ROBIN
          load_assignment:
            cluster_name: admin_loopback
            endpoints:
              - lb_endpoints:
                  - endpoint:
                      address:
                        socket_address:
                          address: 127.0.0.1
                          port_value: {{ .Values.servingEnvoy.edge.adminPort }}
      listeners:
        - name: serving_edge_health_listener
          address:
            socket_address:
              address: 0.0.0.0
              port_value: {{ .Values.servingEnvoy.edge.healthPort }}
          filter_chains:
            - filters:
                - name: envoy.filters.network.http_connection_manager
                  typed_config:
                    "@type": type.googleapis.com/envoy.extensions.filters.network.http_connection_manager.v3.HttpConnectionManager
                    stat_prefix: serving_edge_health
                    codec_type: AUTO
                    route_config:
                      name: serving_edge_health
                      virtual_hosts:
                        - name: health
                          domains: ["*"]
                          routes:
                            - match:
                                path: /server_info
                              route:
                                cluster: admin_loopback
                            - match:
                                path: /ready
                              route:
                                cluster: admin_loopback
                            - match:
                                prefix: /
                              direct_response:
                                status: 403
                    http_filters:
                      - name: envoy.filters.http.router
                        typed_config:
                          "@type": type.googleapis.com/envoy.extensions.filters.http.router.v3.Router
{{- end }}

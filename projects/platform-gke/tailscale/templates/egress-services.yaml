{{- range .Values.egressServices }}
---
apiVersion: v1
kind: Service
metadata:
  name: {{ .name }}
  namespace: {{ $.Values.egressNamespace | default "inference" }}
  annotations:
    tailscale.com/tailnet-fqdn: {{ .tailnetFqdn | quote }}
spec:
  type: ExternalName
  externalName: placeholder.invalid
  ports:
    - name: tcp
      protocol: TCP
      port: {{ .port }}
      targetPort: {{ .targetPort | default .port }}
{{- end }}

# Vendored venv for model-bench agentic verifiers (Task 5 reproducibility).
#
# This is the interpreter that runs REAL MONOLITH CODE inside the pytest verifier
# (fixture code + the repo's own gold tests), NOT the bench harness itself.
# The harness runs on bare python3 (needs: pyyaml, pydantic, httpx, pytest).
#
# Recreate:
#   python3 -m venv ~/.cache/model-bench-venv
#   ~/.cache/model-bench-venv/bin/pip install -r requirements-venv.txt
# Override the location with MODEL_BENCH_VENV=/path/to/venv.
#
annotated-doc==0.0.4
annotated-types==0.7.0
anyio==4.14.1
beautifulsoup4==4.15.0
certifi==2026.6.17
fastapi==0.138.2
h11==0.16.0
httpcore==1.0.9
httpx==0.28.1
idna==3.18
iniconfig==2.3.0
packaging==26.2
pluggy==1.6.0
pydantic==2.13.4
pydantic_core==2.46.4
Pygments==2.20.0
pytest==9.1.1
soupsieve==2.8.4
SQLAlchemy==2.0.51
sqlmodel==0.0.39
starlette==1.3.1
timelength==3.0.3
typing-inspection==0.4.2
typing_extensions==4.15.0

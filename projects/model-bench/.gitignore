# Calibration output: regenerated by `bench run`, machine/run-specific.
results/

# Snapshot-derived fixtures: a pure `git archive` of the pinned commit in each
# task.yaml `snapshot:` block, so the bytes are redundant with the real repo.
# Materialize before a run with `bench snapshot <task-id>`. Hand-written mock
# fixtures (no snapshot block) are still committed; only list snapshot tasks here.
tasks/campsites-region-rollup-01/fixture/

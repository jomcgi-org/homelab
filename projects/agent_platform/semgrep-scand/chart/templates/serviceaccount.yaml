{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "semgrep-scand.serviceAccountName" . }}
  labels:
    {{- include "semgrep-scand.labels" . | nindent 4 }}
{{- end -}}

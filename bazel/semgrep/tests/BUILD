# gazelle:exclude **

# Annotation fixtures (`# ruleid:` / `# ok:`) for the rules in
# //bazel/semgrep/rules. They document what each rule matches; no Bazel target
# runs them since the semgrep_test layer was removed (#4777).

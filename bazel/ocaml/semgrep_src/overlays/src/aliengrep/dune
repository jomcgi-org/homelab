; OVERLAY (bazel/ocaml/semgrep_src/overlays) -- replaces the upstream dune.
;
; One difference from upstream, measured against the pinned sources
; (ocamldep -modules over src/aliengrep; see semgrep_src/README.md):
;   * alcotest dropped: no aliengrep source references Alcotest (its tests
;     subdir has its own dune and stays out of the glob); the git_wrapper
;     pattern. Locking alcotest just to link it stays out of scope.

; New generic mode for Semgrep. Alternative to spacegrep.
(library
 (public_name aliengrep)
 (libraries
   commons
   collections

   logs
   pcre
 )
 (preprocess
   (pps
     ppx_deriving.eq
     ppx_deriving.show
   )
 )
)

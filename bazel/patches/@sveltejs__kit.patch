diff --git a/src/core/sync/write_ambient.js b/src/core/sync/write_ambient.js
index dd798178c86fd6ac4a055269c7d80147b0a7bbb3..ea02583ff74f67920ae872562562f43e48a35f19 100644
--- a/src/core/sync/write_ambient.js
+++ b/src/core/sync/write_ambient.js
@@ -12,12 +12,17 @@ const descriptions_dir = fileURLToPath(new URL('../../../src/types/synthetic', i
 
 /** @param {string} filename */
 function read_description(filename) {
-	const content = fs.readFileSync(`${descriptions_dir}/${filename}`, 'utf8');
-	return `/**\n${content
-		.trim()
-		.split('\n')
-		.map((line) => ` * ${line}`)
-		.join('\n')}\n */`;
+	const filepath = `${descriptions_dir}/${filename}`;
+	try {
+		const content = fs.readFileSync(filepath, 'utf8');
+		return `/**\n${content
+			.trim()
+			.split('\n')
+			.map((line) => ` * ${line}`)
+			.join('\n')}\n */`;
+	} catch {
+		return '/** */';
+	}
 }
 
 /**

actions:
  # Format check - runs formatters and gazelle, auto-commits fixes on PR branches
  # Runs in parallel with Test and Push images
  - name: "Format check"
    container_image: "ubuntu-24.04"
    max_retries: 2
    resource_requests:
      memory: "8GB"
      disk: "20GB"
    triggers:
      push:
        branches:
          - "main"
      pull_request:
        branches:
          - "*"
        merge_with_base: false
    steps:
      - run: |
          # Skip automated commits to avoid loops
          AUTHOR="$(git log -1 --format='%an')"
          if [ "$AUTHOR" = "argocd-image-updater" ] || [ "$AUTHOR" = "ci-format-bot" ]; then
            echo "Skipping automated commit from $AUTHOR"
            exit 0
          fi

          # Run all formatters, generators, and gazelle in a single Bazel invocation.
          # This replaces the previous 3-call approach (bazel build + fast-format.sh + bazel run gazelle)
          # with one gRPC session, reducing transient connection failures.
          bazel run //bazel/tools/format:format --config=ci

          # Validate grep-based generate scripts against bazel query
          ./bazel/images/validate-generate-scripts.sh

          # Reject non-ASCII characters in commit messages.
          # Wrangler Pages (Cloudflare) fails with error 8000111 when commits
          # in the deployed branch contain non-ASCII characters (smart quotes,
          # em-dashes, arrows, etc.). Check all commits since origin/main.
          ./bazel/tools/git/check-commit-msg-ascii.sh --all origin/main

          # Check for formatting changes
          if git diff --exit-code; then
            echo "All files formatted correctly"
            exit 0
          fi

          # On main, fail — formatting must be correct before merge
          current_branch=$(git rev-parse --abbrev-ref HEAD 2>/dev/null || echo "unknown")
          if [ "$current_branch" = "main" ]; then
            echo "ERROR: Code not formatted or BUILD files out of sync."
            echo "Run 'format' locally to fix."
            git --no-pager diff
            exit 1
          fi

          # On PR branches, auto-commit formatting fixes
          echo "Auto-committing formatting fixes..."
          git config user.name "ci-format-bot"
          git config user.email "ci-format-bot@users.noreply.github.com"
          git remote set-url origin "https://x-access-token:${GHCR_TOKEN}@github.com/jomcgi/homelab.git"
          git add -u
          git commit -m "style: auto-format"
          git push origin HEAD:"$current_branch"
          echo "Formatting fixes pushed to $current_branch"

  # Test — runs in parallel with Push images (no dependency between them).
  # PR merge is gated on Test passing; Push images is informational on PRs.
  - name: "Test"
    container_image: "ubuntu-24.04"
    max_retries: 1
    resource_requests:
      cpu: "6"
      memory: "10GB"
      disk: "60GB"
    triggers:
      push:
        branches:
          - "main"
      pull_request:
        branches:
          - "*"
        merge_with_base: false
    steps:
      - run: |
          # Skip automated commits (no code to test)
          AUTHOR="$(git log -1 --format='%an')"
          if [ "$AUTHOR" = "argocd-image-updater" ]; then
            echo "Skipping automated commit from $AUTHOR"
            exit 0
          fi
          bazel test //... --config=ci --deleted_packages=bazel/tools/python --test_tag_filters=-external

          # OCaml multi-arch shard (ADR 006/008): the example/test ladder again,
          # targeting linux/arm64. Toolchain resolution routes ocaml actions to
          # the BuildBuddy cloud arm64 pool (the platform carries Arch: arm64);
          # the sysroot builds from source on that pool, no cross-compilation.
          # Test actions run there too (Bazel selects a test execution platform
          # matching the target platform's constraints). cc_library-dependent
          # examples are tagged no-arm64 until a C++ toolchain targeting
          # aarch64 exists.
          bazel test //bazel/ocaml/... --config=ci \
            --platforms=//bazel/ocaml/platforms:linux_aarch64 \
            --build_tests_only \
            --test_tag_filters=-external,-no-arm64

  # Push images — runs in parallel with Test (no dependency).
  # PR images are tagged with the source branch name (not "main"),
  # so ArgoCD Image Updater ignores them. This ensures image builds are
  # verified before merge. PR merge is still gated on Test passing.
  - name: "Push images"
    container_image: "ubuntu-24.04"
    max_retries: 1
    resource_requests:
      cpu: "6"
      memory: "10GB"
      disk: "60GB"
    triggers:
      push:
        branches:
          - "main"
      pull_request:
        branches:
          - "*"
        merge_with_base: false
    steps:
      - run: |
          # Skip automated commits (digest-only bumps, nothing to push)
          AUTHOR="$(git log -1 --format='%an')"
          if [ "$AUTHOR" = "argocd-image-updater" ]; then
            echo "Skipping automated commit from $AUTHOR"
            exit 0
          fi

          # GHCR_TOKEN set in BuildBuddy Settings → Secrets
          echo "$GHCR_TOKEN" | docker login ghcr.io -u jomcgi --password-stdin
          bazel run //bazel/images:push_all --config=ci --stamp

  # Push pages — runs on main only, in parallel with Test and Push images
  - name: "Push pages"
    container_image: "ubuntu-24.04"
    max_retries: 1
    resource_requests:
      disk: "20GB"
    triggers:
      push:
        branches:
          - "main"
    steps:
      - run: |
          # Skip automated commits (digest-only bumps, no pages to deploy)
          AUTHOR="$(git log -1 --format='%an')"
          if [ "$AUTHOR" = "argocd-image-updater" ]; then
            echo "Skipping automated commit from $AUTHOR"
            exit 0
          fi

          # CLOUDFLARE_API_TOKEN and CLOUDFLARE_ACCOUNT_ID set in BuildBuddy Settings → Secrets
          bazel run //projects/websites:push_all_pages --config=ci

  # Manifest diff — renders Helm manifests from main and PR, posts diff as PR comment
  # Runs in parallel with other actions (no depends_on), non-blocking (informational only)
  - name: "Manifest diff"
    container_image: "ubuntu-24.04"
    max_retries: 1
    resource_requests:
      disk: "20GB"
    triggers:
      pull_request:
        branches:
          - "*"
        merge_with_base: false
    steps:
      - run: ./bazel/helm/ci-diff-manifests.sh

  # Buck2 rules — builds & tests the Buck2 image/helm rules under tools/buck2/**
  # (the Buck2 counterparts to bazel/helm + bazel/tools/oci, consumed by the
  # `loom` repo as an external cell). homelab is otherwise a Bazel repo, so this
  # action self-installs the pinned buck2 release and checks out the buck2
  # prelude submodule before building. Local execution only (no buck2 RE yet).
  # Keep BUCK2_RELEASE aligned with tools/buck2/README.md + loom/.buckconfig.
  - name: "Buck2 rules"
    container_image: "ubuntu-24.04"
    max_retries: 1
    resource_requests:
      cpu: "4"
      memory: "8GB"
      disk: "30GB"
    triggers:
      push:
        branches:
          - "main"
      pull_request:
        branches:
          - "*"
        merge_with_base: false
    steps:
      # POSIX sh-safe (no `pipefail` — the runner shell may be dash). apt is run
      # with sudo only when not already root, so it works regardless of how the
      # BuildBuddy runner is provisioned.
      - run: |
          # Skip bot commits with nothing to build (NOT ci-format-bot — its commit
          # is the PR head, so we must build it or the head shows a false green).
          AUTHOR="$(git log -1 --format='%an')"
          case "$AUTHOR" in
            argocd-image-updater) echo "Skipping automated commit from $AUTHOR"; exit 0 ;;
          esac

          set -eu
          SUDO=""; [ "$(id -u)" -eq 0 ] || SUDO="sudo"

          # Install toolchain deps unconditionally: zstd/curl (fetch+decompress the
          # buck2 release) and build-essential (the cc + libc/crt the prelude's cxx
          # toolchain needs to link the example Rust binary). build-essential is NOT
          # gated on `command -v cc` — a runner can have cc yet lack libc6-dev/crt,
          # which fails the link. The hermetic Rust toolchain brings rustc/std.
          $SUDO apt-get update -qq
          $SUDO apt-get install -y -qq zstd curl build-essential

          # Install the pinned buck2 release (x86_64 linux runner).
          BUCK2_RELEASE="2026-05-18"
          url="https://github.com/facebook/buck2/releases/download/${BUCK2_RELEASE}/buck2-x86_64-unknown-linux-gnu.zst"
          curl -fsSL "$url" -o /tmp/buck2.zst
          zstd -df /tmp/buck2.zst -o /tmp/buck2
          chmod +x /tmp/buck2

          # The buck2 prelude is a submodule excluded from the Bazel checkout.
          git submodule update --init --recursive prelude

          /tmp/buck2 --version
          /tmp/buck2 build //tools/buck2/...
          /tmp/buck2 test  //tools/buck2/...
